// Regenerated at CI build time from the CLOUD_API_BASE repository
// variable (see .github/workflows/android-admin-build.yml) — this
// checked-in copy is just the local-dev/fallback default. Whatever's
// here is only a starting point: the login screen's "Cloud API URL"
// field always overrides it, and the value the person types there is
// what actually gets saved on the device.
window.POS_CONFIG = {
  CLOUD_API_BASE: '',
};
