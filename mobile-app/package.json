{
  "name": "pos-mobile-app",
  "version": "0.1.0",
  "description": "Capacitor wrapper for the customer ordering app, for iOS App Store distribution.",
  "scripts": {
    "sync": "npx cap sync",
    "open:ios": "npx cap open ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  }
}
