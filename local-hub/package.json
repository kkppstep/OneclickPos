{
  "name": "pos-local-hub",
  "version": "0.1.0",
  "description": "Store-side hub: tries the cloud first, falls back to a local queue on timeout, always drives the printer and cash drawer locally.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-thermal-printer": "^4.4.4",
    "uuid": "^9.0.1",
    "axios": "^1.7.7"
  }
}
